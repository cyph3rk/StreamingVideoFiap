server:
    port: 8080
    shutdown: graceful
    servlet:
        encoding:
        charset: UTF-8
        enabled: true
        force: true

# Configuracoes do MongoDB
spring.data.mongodb.host=localhost
spring.data.mongodb.port=27017
spring.data.mongodb.database=streamingvideofiap_db
#spring.data.mongodb.username=admin
#spring.data.mongodb.password=password

# Configuracao do Spring Boot
spring.application.name=sua-aplicacao

